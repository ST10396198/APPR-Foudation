@{
    ViewData["Title"] = "Test Registration";
}

<h2>Test Registration Form</h2>

<p>This is a pre-filled form to test registration.</p>

<div class="mt-3">
    <a href="@Url.Action("Register", "Account")" class="btn btn-primary">Go to Registration Form</a>
    <a href="@Url.Action("DebugUsers", "Account")" class="btn btn-secondary">Check Database Users</a>
</div>